name: Local to Cloud Pipeline

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  smoke-test:
    runs-on: ubuntu-latest
    steps:
      - name: Verify Environment Mechanics
        run: echo "The Github Actions pipeline architecture is functional"